def process_input(user_input):
    """
    Takes user input as a string & returns the result of the action requested.

    Parameters:
       user_input (string): user's input
    """
    pass


def match_to_action(user_input):
    """
    Takes user input & returns the matching action.

    Parameters:
        user_input (string): user's input
    Returns:
        action (function): the action function
    Throws:
        MathcException: exception thrown when a match cannot be found
    """
    pass


def ls(dir_to_list):
    """
    Lists the files and directories in the given directory

    Parameters:
        dir_to_list (string): Path from current directory to directory to list
        files / directories from
    Returns:
        listing (list): A list of the files and directories in dir_to_list
    """
    pass
